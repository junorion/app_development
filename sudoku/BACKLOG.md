# 스도쿠 — 백로그

**위에서부터 하나씩** 진행하고, 끝나면 상태를 고치고 커밋 해시를 적는다.
상태: `대기` · `진행중` · `완료` · `보류` · `확인필요`

항목은 두 묶음으로 나눈다. 지금 앱은 **인터넷 권한이 없고, 개인정보를 수집하지 않으며,
광고가 없다**. 1군은 그 성격을 그대로 두고 할 수 있는 것이고, 2군은 그 성격을 바꾸는 것이라
스토어 신고 서류까지 다시 써야 한다 (아래 "2군에 들어가기 전에" 참고).

---

## 0. 결함 — 완료

| # | 항목 | 상태 | 커밋 |
|---|---|---|---|
| 0-1 | 기록 지우기에 확인 절차가 없어 한 번 누르면 영구 삭제 | **완료** | `2b71c22` |
| 0-2 | 저장 데이터가 손상되면 앱이 죽고, 데이터가 남아 계속 죽음 | **완료** | `2b71c22` |
| 0-3 | `S` 가 없을 때 메모 버튼 핸들러에 방어 없음 | **완료** | `2b71c22` |
| 0-4 | 난이도 판정이 실질적으로 무력했음 — 기법 등급을 상한으로만 써서 "전문가" 10개 중 4개가 숨은 홑수만으로 풀렸다 | **완료** | 기법 하한(`RATE_MIN`)을 함께 두고 테스트로 고정 |
| 0-5 | `saveGame()` 이 진행 중인 게임이 없을 때 저장본을 지웠다 — Home 이 시작 화면이 되면서 **앱을 켤 때마다 이어하기가 사라지는** 버그가 됐다 | **완료** | 끝난 게임만 지운다. 회귀 테스트 추가 |
| 0-6 | `engine.test.js` 가 힌트 작업 이후 깨진 채 방치됐다 (스크래치에 있어 눈에 띄지 않음) | **완료** | 테스트를 `sudoku/test/` 로 옮기고 `run.js` 로 한꺼번에 돌린다 |
| 0-7 | **가로로 누운 폰(740×360)에서 화면이 깨졌다** — 가로 레이아웃 분기점이 820px 이라 세로용 레이아웃을 써서 칸이 11px 로 찌그러졌고, Home 은 442px 로 쌓여 스크롤이 생겼다 | **완료** | 분기점을 660px 로 낮추고, Home 은 낮고 넓은 화면에서 2열로 접는다. 숫자 키 높이를 `min(60px, (100vh-200px)/3)` 으로 화면에 맞춘다. 6개 뷰포트 × 8개 화면 전부 넘침 0 으로 확인 |
| 0-8 | `#live`(스크린리더 알림 영역)가 `position:absolute` 에 좌표가 없어 문서 흐름 자리에 놓였고, 모든 화면이 1px 씩 길어져 스크롤이 생겼다 | **완료** | `position:fixed; top:0; left:0` |
| 0-9 | 끝난 판에서 되돌리기·다시하기가 동작해 기록과 화면이 어긋났다. 다 쓴 숫자 키가 눌렸다 | **완료** | `S.done` 이면 무시하고, 9개를 다 채운 숫자 키는 `disabled` |
| 0-10 | **`[hidden]` 이 듣지 않았다** — `.btn { display: flex }` 처럼 display 를 지정한 클래스가 `[hidden]` 의 UA 기본값을 이겨서, `el.hidden = true` 로 숨긴 것이 그대로 보였다 (코인이 없을 때 힌트 "적용" 버튼이 남아 있었다) | **완료** | `[hidden] { display: none !important; }` 하나로 못박고 개별 규칙 3개를 지웠다. `hidden` 을 쓰는 곳이 17군데다 |
| 0-11 | 코인이 0일 때 힌트 버튼이 `disabled` 라 클릭이 일어나지 않았고, 준비해 둔 "코인이 모자랍니다" 안내가 **뜰 수 없었다** | **완료** | `aria-disabled` 로 바꿔 흐리게만 두고, 누르면 이유를 설명한다 |

---

## 1군. 지금 구조에서 가능 — 오프라인·무광고·무수집 유지

| # | 항목 | 상태 | 메모 |
|---|---|---|---|
| 1-1 | **영어 지원** — UI 문자열, 안드로이드 앱 이름, 스토어 등록정보 영어판 | **완료** (`versionCode 2`) | 기기 언어로 자동 선택 + 헤더 버튼으로 수동 전환. 앱 이름은 기본 `Sudoku`, 한국어 기기에서 `스도쿠`. 영어 스크린샷·등록정보까지 준비 |
| 1-2 | **UI 를 상용앱 수준으로** — 화려함보다 심플함, 단 고퀄리티 | **1차 완료** (`versionCode 4`) | 앱 이름을 "브레인 스도쿠 도쿠도쿠" 로 정하고 그 컨셉(앞은 지적·전문적, 뒤는 친근함)에 맞춰 다시 그렸다. 목재·종이·세리프를 걷어내고 남색 바탕 + 인디고 강조 + 고딕 숫자로 바꿨다. 보드도 테마를 따라간다. 한 번에 끝나는 항목이 아니므로 이후에도 기준으로 삼는다 |
| 1-3 | **풀이법을 설명하는 힌트** — 답만 주지 않고 "왜 그 숫자인지" 설명 | **완료** | 틀린 숫자 → 숨은 홑수 → 홑수 → (그 외는 공개) 순서로 다음 한 수를 찾아 근거와 함께 설명한다. 대상 칸·근거 칸·해당 단위를 보드에서 색으로 짚어 주고, 아래쪽 바에 설명을 띄운다(전체 오버레이로 하면 짚어 준 칸이 가려진다). 숫자는 "적용"을 눌러야 들어간다. 한국어 조사는 숫자 받침에 따라 갈린다(1·3·6·7·8 은 받침 있음) |
| 1-4 | **힌트를 소모성 자원으로** + 게임 완료 시 코인 1개 지급 | **완료** | 코인으로 구현했다. 힌트를 **볼 때** 1개 쓴다(설명 자체가 답을 드러내므로 적용 시점이 아니다). 완료하면 1개 번다. 코인은 통계와 **별도 키**에 둔다 — "기록 지우기"로 화폐가 사라지면 안 된다. **`INITIAL_COINS` 는 기능 시험용 999 다. 출시 전에 낮춰야 한다** |
| 1-5 | **월단위 데일리 게임** + **Home 화면**(새 게임/데일리 선택, 스코어·레벨 표시) | **완료** | 날짜를 씨앗으로 쓰는 결정론적 생성(`mulberry32` + FNV-1a)이라 서버 없이 모든 기기가 같은 날 같은 문제를 받는다. 난이도는 요일로 돌린다(`DAILY_DIFF`). 앱은 Home 으로 시작하고 이어하기 / 새 게임 / 데일리 / 기록을 고른다. 월 달력에서 지난 날짜도 풀 수 있고(내일은 못 푼다) 완료한 날이 표시된다. 점수는 난이도 기본값 × 속도 × 힌트 감점, 데일리는 1.5배. 누적 점수로 레벨이 오른다 |
| 1-6 | **자동 노트 기능** + 게임 시작 시 사용 여부 선택 | **완료** | 난이도 선택 시트의 스위치로 켜고 끈다(설정이 저장된다). 켜면 새 게임·데일리 시작 시 모든 빈칸에 후보를 적어 둔다. 2군에서 코인·멤버십으로 팔 때는 이 스위치 한 곳만 막으면 된다 |
| 1-7 | 난이도별 문제 생성기 **오프라인 대비** (비행기 모드) | **완료 (검증)** | 검증 완료 — 앱 안에 네트워크 호출 0건, 외부 리소스 0건(아이콘·manifest 모두 APK 내장), APK 에 인터넷 권한 자체가 없다. 생성은 `Math.random()` 과 순수 계산만 쓴다. 비행기 모드에서 네 난이도 모두 그대로 만들어진다 |
| 1-8 | 설정 화면 | **완료** | Home 의 톱니 버튼. 숫자 먼저 고르기 · 메모 자동 채우기 · 진동 · 오류 표시 · 시간 표시 다섯 가지 |
| 1-9 | 첫 실행 사용법 안내 | **완료** | 첫 실행에 한 번 뜨고, 이후에는 Home 의 물음표 버튼으로 언제든 볼 수 있다. 입력·규칙·메모·힌트·데일리 다섯 가지 |
| 1-10 | 입력 편의 — 숫자 우선 모드 | **완료** | 설정에서 켠다. 고른 숫자가 패드에서 강조되고 같은 숫자가 든 칸이 함께 보인다. 같은 숫자를 다시 누르면 해제 |
| 1-25 | **배경을 흰색으로, 숫자 글꼴을 Andika 로** | **완료** | 라이트 테마 배경 `#eef1f8`→`#ffffff`. 카드도 흰색이라 경계가 사라져 테두리와 격자선을 조금 진하게 잡았다. 숫자 글꼴은 Andika(SIL) — 1 과 7, 6 과 9 가 헷갈리지 않게 그려진 글꼴이다. 인터넷 권한이 없어 받아 올 수 없고 `file://` 에서 바깥 파일을 `@font-face` 로 부르면 막히는 일이 있어, latin 서브셋 woff2 두 벌(38KB)을 base64 로 심었다. 보드·숫자판·메모에 쓴다. OFL 1.1 이라 전문을 `fonts/OFL-Andika.txt` 에 두고 설정 아래에 고지를 넣었다 |
| 1-33 | **기본 글꼴을 프리텐다드 ExtraLight 200 으로** | **완료** | 저장 키를 v2 로 올렸다 — 앞선 버전에서 글꼴을 한 번이라도 만진 기기에는 옛 값이 남아 새 기본이 보이지 않는다. 값을 정하는 중이라 한 번 비우고 간다 |
| 1-34 | **다시하기 · 남은 개수 · 숫자 키 테두리 제거** | **완료** | 다시하기는 버튼과 `S.future` 까지 걷었다(되돌린 것을 담아 둘 곳이 없다). 숫자판의 남은 개수와 그 설정 행도 뺐다 — 다 쓴 숫자를 흐리게 두는 것은 남긴다. 숫자 키는 테두리를 없애고 보드 숫자와 같은 글꼴·두께·크기를 쓴다. 배경은 패널색 대신 한 단계 옅은 색으로 바꿨다 — 흰 바탕에서 흰 패널은 누를 곳이 보이지 않는다 |
| 1-31 | **글자 크기 설정** | **완료** | 숫자는 칸의 55~85%, 메모는 20~32% 를 일곱·다섯 단계로 고른다. 절대 크기가 아니라 칸에 대한 비율이라, 화면이 커져 칸이 커지면 글자도 함께 커진다. 가장 크고 굵은 조합(85% · Black 900)에서도 숫자가 칸을 벗어나지 않는 것을 7개 뷰포트에서 확인했다 |
| 1-32 | **간헐적으로 깨지던 엔진 테스트** | **완료** | "해가 갈라지는 배치를 찾았다" 가 무작위 완성판 한 판만 보고 판정해서, 그런 배치가 없는 판이 나오면 이유 없이 실패했다. 최대 20판을 훑는다 |
| 1-30 | **글꼴 두께 설정** | **완료** | Thin 100 ~ Black 900 아홉 단계. 심어 둔 두 글꼴이 가변이라 모두 실제로 나온다(기기 글꼴은 가장 가까운 쪽으로 붙는다). 숫자와 메모를 따로 고르고, 문제로 주어진 숫자는 고른 값보다 두 단계(+200, 900 상한) 굵게 그려 어느 두께에서도 구분이 남는다 |
| 1-28 | **설정에서 글꼴을 바꿀 수 있게** | **완료** | 개발 중에 눈으로 비교하려는 것이다. 숫자 글꼴과 메모 글꼴을 따로 고른다(본고딕 · 프리텐다드 · 시스템 · 명조 · 고정폭 · 둥근고딕). 심어 둔 것은 본고딕(26KB)과 프리텐다드(38KB) 둘이고 나머지는 기기에 있는 것을 쓴다. **출시 전에 숫자 글꼴 행을 남길지 정한다** |
| 1-29 | **헤더·도구가 좁은 화면에서 두 줄로 접히던 문제** | **완료** | 헤더와 도구·패드가 보드 폭(`--cell*9+14`)에 묶여 있어, 칸이 작아지면 "쉬움"이나 "되돌리기"가 두 줄이 됐다. 최소 폭 330px 을 줬다. 가로 배치에서는 옆 칸이 좁으므로 최소 폭을 풀고 도구 글자 크기도 고정한다 — 태블릿 가로에서 도구 이름이 접혔다. 세로 여유값도 200→232 로 맞췄다 |
| 1-27 | **숫자 글꼴을 본고딕으로** | **완료** | Andika 를 본고딕(Noto Sans KR)으로 갈았다. 숫자만 쓰므로 한글 글리프가 없는 latin 서브셋 하나(26KB)면 되고, 가변 글꼴이라 그 파일 하나로 100~900 굵기가 다 나온다 — Andika 는 400·700 두 벌(38KB)이었다. 중간 굵기가 실제로 있어 주어진 숫자를 600 으로 되돌렸다 |
| 1-26 | **숫자를 가볍고 크게** | **완료** | Andika 는 400·700 두 벌뿐이라 500·600 을 주면 Bold 로 튀거나 합성된다 — 화면 숫자 대부분이 실제 볼드였다. 굵기를 400 으로 내리고 크기를 칸의 .52→.70 배로 키웠다. 문제로 주어진 숫자는 굵기 대신 색으로만 가른다. 숫자판도 400 에 칸 크기 비례로 키웠다. 메모만 700 을 남겼다 — 아주 작아서 가늘면 읽히지 않는다 |
| 1-21 | **설정에서 메모 자동 채우기 제거** | **완료** | 난이도 시트의 "자동 메모 사용 시작" 버튼이 그 역할을 한다. 저장 키와 기능은 그대로 두고 설정 행만 뺐다 |
| 1-22 | **실수 제한** | **완료** | 기본 켬, 세 번까지(`MISTAKE_MAX`). 헤더에 `1/3` 로 세고 마지막 한 번이 남으면 빨갛게 바뀐다. 넘기면 판이 끝나고 점수·코인 없이 연속 완료가 끊긴다. 되돌리기로는 실수를 무르지 못한다 — 무를 수 있으면 제한이 없는 것과 같다. 판정은 규칙 위반이 아니라 **정답과 다른 값**이다 |
| 1-23 | **Auto-Remove Notes 를 설정으로** | **완료** | 동작 자체는 있었고 끄고 켤 수단이 없었다. `autoClearNotes` 로 뺐다(기본 켬). 손으로 놓을 때와 힌트로 채울 때 모두 이 설정을 따른다 |
| 1-24 | **효과음·배경음·진동을 하나로** | **완료** | `feedback` 한 스위치로 묶었다(기본 켬). 배경음이 기본으로 흐르게 된 셈이라 볼륨을 낮게 잡아 두었다 |
| 1-14 | **게임 화면을 기기 화면 크기에 맞춘다** | **완료** | 칸 크기 상한(44·46·54px)을 걷고 남는 공간으로만 정한다. 세로는 보드 9칸 + 패드 1.25칸을 함께 계산한다(패드도 칸을 따라 커진다). 800×1280 태블릿에서 보드가 414 → 766px 이 됐다. 도구·헤더 버튼도 칸 크기를 따라 커지게 했다 — 보드만 키우면 태블릿에서 납작해 보인다 |
| 1-15 | **홈 외 모든 화면에 상단 ← / X** | **완료** | 공용 `.sheet-bar` 를 만들어 여섯 시트에 넣고, 시트마다 다르던 하단 닫기 버튼은 걷었다. 바는 sticky 라 설정처럼 긴 시트를 스크롤해도 붙어 있다. 게임 헤더에도 X 를 더했다. 확인 대화상자·생성 중·일시정지는 뺐다 — 선택을 강제하거나 진행 중인 표시다 |
| 1-16 | **메모 글자를 진하게** | **완료** | 라이트 `#98a1b6`→`#6b7591`, 다크 `#7c869e`→`#a8b3cc`. 크기도 칸의 .24→.26 배, 굵기 600→700 |
| 1-17 | **설정으로 제어 가능한 것 정리** | **완료** | 화면에 박혀 있던 셋을 뺐다 — 줄·박스 표시, 같은 숫자 표시, 숫자판의 남은 개수. 언어·화면과 함께 설정이 14행이 됐다 |
| 1-18 | **효과음 · 배경음** | **완료** | 음원 파일 0바이트 — WebAudio 로 그때그때 만든다. 효과음 여섯 가지(놓기·지우기·틀림·힌트·탭·완료), 배경음은 가락 없는 화음에 아주 느린 흔들림만 준다(몇 분 만에 물리지 않게). 배경음은 게임을 보고 있을 때만 흐르고 홈·일시정지·시트에서 멈춘다. 효과음은 기본 켬, 배경음은 기본 끔 |
| 1-19 | **메모 폰트 선택** | **완료** | 고딕·명조·고정폭·둥근고딕. 기기에 있는 글꼴만 쓴다 — 웹폰트를 받으면 인터넷 권한이 필요해져 오프라인이 깨진다 |
| 1-20 | **테마 색 지정** | **완료** | 여섯 가지(인디고·청록·초록·자주·호박·회청). 색마다 밝게/어둡게 값과 그 위에 얹을 글자색을 따로 정해 대비를 맞췄다. 설정에서 스와치로 고른다 — 값 순환보다 눈으로 고르는 편이 빠르다 |
| 1-13 | **게임 화면 정리** — 하단 새 게임 제거(광고 자리로 예약) · 상단 뒤로 가기 · 상단 오른쪽 옵션 · 힌트 남은 개수 | **완료** | 게임 중에 하단 "새 게임"을 잘못 눌러 판이 날아가는 일이 있어 없앴다. 그 자리는 2군 배너(320×50)를 위해 비워 두되 **높이는 지금부터 차지한다** — 나중에 광고가 들어와도 보드가 밀리지 않는다. 광고가 붙기 전까지 **빈 여백으로 둔다** (2026-09-10 확정 — 감추지 않는다). 헤더는 홈 아이콘을 뒤로 화살표로 바꾸고 톱니(옵션)를 더했다. 자리를 내주려고 헤더의 언어·테마 버튼은 옵션 안으로 옮겼다(값을 고르는 행 두 개). 힌트 개수는 전구 옆 작은 배지에서 라벨과 같은 줄의 색 있는 알약으로 바꿨다 — 무엇의 개수인지 읽히지 않았다 |
| 1-12 | **난이도 선택 시트 정리** — 난이도는 고르기만 하고, 시작은 아래 두 버튼으로 | **완료** | 난이도 이름을 가운데 정렬하고 "빈칸 xx개"를 없앴다. 고르면 바로 시작하던 것을 선택만 하도록 바꾸고, 아래에 "시작" / "자동 메모 사용 시작" 두 버튼을 뒀다(자동 메모 스위치를 대체한다). 취소 버튼은 우상단 X 로 바꿨다. 지난번에 고른 난이도를 기억한다(`KEY_LASTDIFF`) |
| 1-11 | 접근성 — 스크린리더 지원 | **1차 완료** | 칸마다 "3행 5열, 7" 같은 이름과 행/열 인덱스, 선택 상태, given 여부를 넣었다. 탭 순서는 고른 칸 하나만(roving tabindex). 힌트·완료는 화면 밖 알림 영역으로 읽힌다. 실제 스크린리더 검증은 실기기에서 필요 |

---

## 2군-A. 수익화 — **진행** (2026-09-11 결정)

서버 없이 끝난다. 광고는 AdMob 이, 결제는 Play 가 처리한다. 설계와 준비물은
**[DESIGN-2.md](DESIGN-2.md)** 에 있다. 위에서부터 하나씩.

| # | 항목 | 상태 | 밖에서 필요한 것 |
|---|---|---|---|
| A-1 | 인터넷 권한 + AdMob SDK, **테스트 광고 ID** 로 배너 (2-4) | 대기 | 없음 — 지금 시작할 수 있다 |
| A-2 | 오프라인에서 광고만 빠지고 게임은 그대로인지 확인 | 대기 | 없음 |
| A-3 | Play 결제 연결, 멤버십 1종(1달) (2-7) | 대기 | 판매자 프로필 · 인앱 상품 등록 |
| A-4 | 광고 제거 · 힌트 무제한을 멤버십에 연결 (2-6) | 대기 | 없음 |
| A-5 | 보상형 광고로 코인 벌기 (+ D2 가 정해지면 2-5) | 대기 | 보상형 광고 단위 |
| A-6 | 서류 다시 쓰기 → 실 광고 ID 교체 → 업로드 | 대기 | 실 광고 ID |

**정해야 할 것**: D2 — 자동 메모(2-5)를 멤버십/코인으로 옮길 것인가. 지금 무료인
기능이라 옮기면 빼앗는 것으로 보인다. A-5 전까지 정하면 된다.
**정해 둔 것**: D1 "초기 5번" = 광고 5회 면제권(잠정) · D5 배너 + 보상형, 전면 없음.

## 2군-B. 온라인 — **보류**

서버·계정·부정 점수 방지가 필요하고 앱의 성격이 바뀐다. A 를 끝내고 다시 본다.

| # | 항목 | 상태 | 메모 |
|---|---|---|---|
| 2-1 | **난이도별 점수 획득 구조** | **로컬 부분 완료** | 점수·레벨은 1-5 에서 기기 안에 구현했다(`scoreFor`, `levelOf`). 남은 것은 등수와 엮는 부분뿐이고 그때 서버가 필요하다 |
| 2-2 | **주간 등수** | 보류 | 서버 + 사용자 식별. 부정 점수 방지가 절반이다 — 데일리 퍼즐 하나로 한정해 시작하는 안을 DESIGN-2.md 3.2 에 적었다 |
| 2-3 | **대결** | 보류 | 실시간 매칭이 2군에서 가장 비싸다. 비동기 대결로 시작하는 안을 DESIGN-2.md 3.3 에 적었다 |

### 출시 전에 반드시 확인할 것

| 항목 | 내용 |
|---|---|
| ~~`INITIAL_COINS`~~ | **완료** — 10 으로 낮췄다. 힌트가 어떤 것인지 겪어 볼 만큼이면서 매 판 힌트로 푸는 습관이 들 만큼은 아닌 값. 그 뒤로는 한 판 끝낼 때마다 하나씩 번다 |
| ~~스토어 자료~~ | **완료** — 스크린샷을 한국어 7장 + 영어 7장으로 다시 찍었다(홈 · 힌트 · 게임 · 데일리 · 다크 · 완료 · 기록). 규격도 고쳤다: 예전 864×1920 은 종횡비 2.22:1 이라 Play 의 2:1 제한을 벗어나 있었고, 지금은 1080×1920 이다. `capture.sh` / `shots.html` 로 언제든 다시 찍는다. 영어용 그래픽 이미지(`feature-graphic-en.png`)도 새로 만들었다(그전에는 한글이 박힌 이미지를 영어 등록정보에 쓰게 되어 있었다). 등록정보 문안도 데일리·점수·코인·자동 메모·설정을 넣어 다시 썼다 |
| `versionCode` | 업로드할 때마다 올린다. **지금은 19 (`versionName 2.8`)** — 위 세 가지를 반영해 빌드해 두었다 |

## 2군에 들어가기 전에 알아둘 것

> 설계와 준비 체크리스트는 **[DESIGN-2.md](DESIGN-2.md)** 에 따로 정리했다.
> 2군을 수익화(A, 서버 없음)와 온라인(B, 서버 필요) 두 덩어리로 나누고, 사람이
> 직접 해야 하는 준비(계정·상품 등록·테스터 모집)와 먼저 정해야 할 것(D1~D5)을
> 적어 두었다. 아래는 그 요약이다.

지금까지 만든 스토어 자료는 "인터넷 권한 없음 · 수집 없음 · 광고 없음"을 전제로 쓰여 있다.
2군을 시작하면 아래가 **전부 다시 작성**된다.

- `sudoku/privacy.html` — 수집 항목, 광고 파트너, 제3자 제공을 명시해야 한다
- Play **데이터 보안 양식** — "수집 안 함"에서 "수집함"으로 바뀌고 항목별 신고가 필요하다
- Play **콘텐츠 등급** — 사용자 간 상호작용(대결)이 생기면 등급 설문 답이 달라진다
- **타깃 연령** — 광고·결제가 붙으면 아동 대상 정책을 더 신경 써야 한다
- `AndroidManifest.xml` 에 인터넷 권한 추가, 앱 크기·오프라인 동작 재검토
- 서버 운영 비용과 개인정보 보관 책임이 생긴다

**권장 순서**: 1군을 끝내고 그 상태로 먼저 출시한 뒤, 2군은 다음 버전으로 올린다.
1군만으로도 완결된 앱이고, 2군은 되돌리기 어려운 결정을 여럿 포함한다.

---

## 진행 규칙

- 한 항목이 끝날 때마다 검증하고 커밋한다 — 로직은 `node sudoku/test/run.js`,
  화면은 헤드리스 Chrome 스크린샷.
- 웹앱을 고치면 안드로이드는 다시 빌드만 하면 된다 (`syncWebApp` 이 자산을 복사한다).
- 스토어에 올릴 AAB 를 다시 만들 때는 `versionCode` 를 올린다.
